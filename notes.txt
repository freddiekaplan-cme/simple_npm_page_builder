replace
header
style för menu

meny
se till att aktuell sida har annan färg

temporal YEAR &copy;

js för meny

/scripts/menu.js

fs folder skapa replace " " med _ gör lower case namn på mapp från titel annars "site"

const fs = require('fs');

// Create a folder
fs.mkdir('./${folderName}', { recursive: true }, (err) => {
  if (err) throw err;
  console.log('Folder created!');
});

Replace path/to/folder with the desired folder path. The recursive: true option will create the folder and any necessary parent folders if they don't already exist.

År
const currentDate = new Date();
const currentYear = currentDate.getFullYear();

console.log(currentYear);